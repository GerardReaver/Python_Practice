# define a function that accepts two arguments (length and width), calculates the area of the rectanle (length * width), and prints the result. 
# After definint the function, prompt the user for the length and the width inputs and then call your function with these inputs. 
def calculate_area(length, width):
    # Write your code below
    print(length * width) 

length = float(input())
width = float(input())
# Call the function below
calculate_area(length, width)
