# Create a function called "square_number" that takes a single paramenter "n" and returns its square. 
# Then, call teh function with the input value and store the result in a variable called "result". 
# Finally, print the value of "result". 
def square_number(n):
    print(n * n)
n = int(input())

square_number(n)
